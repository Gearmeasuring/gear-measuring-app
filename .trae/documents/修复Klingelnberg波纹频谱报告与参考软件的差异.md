## 问题分析
通过对比当前代码生成的频谱图表与参考软件的结果，发现以下关键问题：

1. **候选阶次生成不完整**：当前代码仅生成ZE的整数倍数作为候选阶次，而参考软件显示的频谱包含更多阶次
2. **闭合曲线构建不准确**：轴向坐标到旋转角的转换可能存在偏差
3. **正弦拟合算法差异**：拟合实现与参考软件的克林贝格方法不一致
4. **数据处理流程参数需要调整**：去均值、去趋势、crowning correction等步骤的参数需要优化
5. **幅值计算方法需要修正**：当前计算可能与参考软件存在偏差
6. **阶次选择逻辑不匹配**：主导阶次选择方式与参考软件不同
7. **图表显示格式问题**：最终图表的视觉呈现与参考软件不一致

## 修复计划

### 1. 优化候选阶次生成（klingelnberg_ripple_spectrum.py）
- 修改 `_calculate_spectrum` 方法中的候选阶次生成逻辑
- 确保生成ZE倍数及其邻近阶次（±5范围）
- 修复代码行：1410行

### 2. 改进闭合曲线构建（klingelnberg_ripple_spectrum.py）
- 优化 `_build_common_closed_curve_angle` 和 `_build_helix_closed_curve_angle` 方法
- 提高轴向坐标到旋转角转换的准确性
- 修复代码行：512行、567行

### 3. 修正正弦拟合算法（klingelnberg_ripple_spectrum.py）
- 统一使用克林贝格标准正弦拟合方法
- 优化 `_sine_fit_spectrum_analysis` 方法
- 确保拟合结果与参考软件一致
- 修复代码行：275行

### 4. 调整数据处理流程参数（klingelnberg_ripple_spectrum.py）
- 优化去均值、去趋势、crowning correction等步骤的参数
- 确保数据处理结果与参考软件一致
- 修复代码行：1336行

### 5. 修正幅值计算方法（klingelnberg_ripple_spectrum.py）
- 调整幅值计算逻辑，确保与参考软件一致
- 修复代码行：1286行、1532行

### 6. 优化阶次选择逻辑（klingelnberg_ripple_spectrum.py）
- 修改 `_select_dominant_orders` 方法，匹配参考软件的阶次选择方式
- 确保主导阶次选择与参考软件一致
- 修复代码行：788行

### 7. 修复图表显示格式（klingelnberg_ripple_spectrum.py）
- 调整频谱图表的显示逻辑，确保视觉呈现与参考软件一致
- 修复代码行：1611行

### 8. 检查调用参数传递（klingelnberg_single_page.py）
- 确保调用波纹频谱报告生成器时传递了正确的参数
- 修复代码行：215-217行

## 预期效果
修复后，生成的频谱图表将与参考软件的结果一致，包括：
- 阶次分布与参考软件相同
- 幅值大小与参考软件一致
- 图表视觉呈现与参考软件匹配
- 表格数据与图表数据一致

## 实施步骤
1. 首先修复核心的频谱计算逻辑
2. 然后优化数据处理流程
3. 最后调整图表显示格式
4. 进行测试验证，确保结果与参考软件一致