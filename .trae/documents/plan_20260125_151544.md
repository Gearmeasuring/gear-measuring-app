**继续修复Klingelnberg波纹频谱报告**

**当前状态**：
- 已修改阶次选择范围，将ZE倍数扩展到7ZE，确保覆盖最高阶次609和610
- X轴标记设置正确，只显示2ZE到6ZE，与参考软件一致
- X轴范围设置为7*teeth_count，能够显示所有高阶次

**下一步计划**：

1. **完整测试流程**：
   - 运行完整的频谱生成流程，使用实际测试数据
   - 验证所有参考图表中显示的阶次都能被正确识别和显示
   - 检查振幅值是否与参考软件一致

2. **调整图表样式**：
   - 确保图表的视觉样式与参考软件完全一致
   - 检查坐标轴范围、标记样式、字体大小等细节
   - 验证右侧参数显示（ep/lo/lu或el/zo/zu）是否正确

3. **生成最终报告**：
   - 使用测试数据生成完整的PDF报告
   - 对比生成的报告与参考软件的输出
   - 确保所有数值和视觉元素都匹配

**预期结果**：
修改后的代码将生成与参考软件完全一致的Klingelnberg波纹频谱报告，显示所有相同的阶次和振幅值，以及相同的视觉样式。